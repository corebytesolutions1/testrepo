node_modules/
.env
logs/*.log

# Uploaded content — runtime data, not source. Keep the folders themselves
# (via .gitkeep) but never commit what gets uploaded through the admin panel.
public/pdf/*
!public/pdf/.gitkeep
public/uploads/*
!public/uploads/.gitkeep

.DS_Store
*.log
