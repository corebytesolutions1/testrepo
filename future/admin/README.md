# admin module (planned)

Reserved folder for the future "admin" feature described in the master project brief.
Not yet implemented. Suggested next steps when building this out:

- Add routes under routes/admin.js and mount in server.js
- Add a model in models/ and a controller in controllers/
- Add auth/session middleware if this module requires login
- Add views under views/admin/
